### Source file structure
- `rlr` contains the headers and source files for the public api.
- `internal` contains all the files for the internal implementation of the renderer and should not be used by anyone using the library.
- `external` contains implementation files for the single header file libraries.